///Checking Env
pub fn check_env()->Option(String){
	use std::env;

// Prints each argument on a separate line
pub for argument in env::args() {
    println!("{}", argument);
}

}

pub fn split_by_size(){
	
}

pub fn split_by_dim(){
	
}