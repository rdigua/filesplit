use std::io;
use std::io::prelude::*;
use std::fs::File;
use std::io::Error;
use std::env;
use super::conf;

pub fn split_by_size(s:&str,ref c:conf::Config) {
    unimplemented!()
}

pub fn split_by_dim() {
    unimplemented!()
}